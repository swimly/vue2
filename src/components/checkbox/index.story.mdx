import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import MCheckbox from './src/checkbox.vue'

<Meta
  title="组件/MCheckbox 按钮"
/>

## Checkbox按钮

按钮用于开始一个即时操作。

### 按钮类型

按钮有五种类型：`default`，`primary`，`warning`，`success`，`danger`，默认是：`default`。

<Story name='线条按钮' height='40px'>
  {{
    components: { MCheckbox },
    template: `
      <div>
        <m-checkbox>按钮</m-checkbox>
      </div>
    `,
  }}
</Story>